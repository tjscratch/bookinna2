/*import React from 'react';

import { Link } from 'react-router'

const Layouts = (props) => {
    return(
         <Link to={`${props.offerUrl}`} target="_blank" className="best-offers" >
           <li className="offer-list" style={{background: `url(${props.offerImgUrl}) center/cover no-repeat`}}>
                <div className="offer-data-container">
                    <span className="offer-small">{props.offerCountry}</span>
                    <span className="offer-big">{props.offerCity}</span>
                    <span className="offer-small">{props.offerPeriod}</span>
                </div>
                <div className="offer-price-container">
                    <span className="offer-small">от</span>
                    <span className="offer-big">{props.offerPrice} <i className="offer-big fa fa-rub"></i></span>
                </div>
            </li>
         </Link>
    )
};

export default Layouts;*/